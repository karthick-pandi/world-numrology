export const ROUTES = {
  LOGIN: "/login",
  DAILYFORCAST: "/index/daily-forecast",

};

export interface SavedReading {
  id: string;
  image: string;
  userName: string;
  reportName: string;
  date: string;
  color: string;
}

export const CONST = {
  ROUTES,
};
